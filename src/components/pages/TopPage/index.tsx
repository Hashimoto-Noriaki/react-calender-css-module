import styles from './styles.module.css';

export const TopPage = () => {
  return (
    <div className={styles.container}>
      <h1 className={`${styles.text3xl} ${styles.fontBold} ${styles.underline}`}>
        犬
      </h1>
    </div>
  );
};
